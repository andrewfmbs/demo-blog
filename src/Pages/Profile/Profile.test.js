import React from "react";
import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import Profile from "./Profile";

describe("profile renders", () => {
  test("display username", () => {});
  test("display user data", () => {});
  test("display user recent posts", () => {});
  test("display recent comments", () => {});
  test("display recent user like activity", () => {});
  test("display recent user posts' like activity", () => {});
});
